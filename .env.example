# Copy to .env for local dev. NEVER commit .env.
# In CI these live in GitHub repo Settings -> Secrets and variables -> Actions.

DATABASE_URL=postgresql://user:password@host:5432/dbname
SECTORS_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
